// js/paquetes.js
import { sendData } from './api.js'; 

export function initPaquetes() {
    console.log('Paquetes: Lógica de formulario inicializada.');
    const formPaquetes = document.getElementById('form-paquetes');
    // Agrega aquí los listeners y la lógica de envío para Paquetes
}